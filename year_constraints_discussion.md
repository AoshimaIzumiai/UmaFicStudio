# 年份算法约束讨论

## 模式切换

系统提供两种模式，用户可在设置中一键切换：

### 严谨现实模式
- 所有年份相关约束**强制生效**
- 不满足条件时阻止保存或给出警告（取决于下方问题5的回答）
- 适合追求真实感的创作者

### 完全架空模式
- **忽略全部年份相关约束**
- 任何年份组合都可以保存，不做校验
- 适合自由创作、不想受年份限制的玩家

> 注：以下所有规则仅在「严谨现实模式」下生效。

---













## 已确定的规则

### 规则 1：牝马最早产驹年龄
- 牝马必须满 3 岁后才能生下第一匹小马
- 即：**后代出生年 ≥ 母亲出生年 + 4**（3岁配种 → 怀孕11个月 → 4岁时产驹）

### 规则 2：种马配种年份范围
- 种马只能在 `stud_year_start` 至 `stud_year_end` 期间被选为父亲
- 即：**后代出生年 ≥ 种马 stud_year_start + 1**（当年配种 → 次年出生）
- 如果 `stud_year_end` 有值：**后代出生年 ≤ 种马 stud_year_end + 1**

---

## 需要确认的规则

### 问题 1：种马最早配种年龄
种马一般从几岁开始配种？是否需要约束种马的最小年龄？

**情景：** 一匹 2020 年出生的架空种马，用户想让它 2023 年（3岁）就开始配种产驹。

- A. 允许，不限制种马最小配种年龄（由 stud_year_start 字段控制）
- B. 强制最小 3 岁开始配种（stud_year_start ≥ birth_year + 3）
- C. 强制最小 4 岁（更符合日本现实）

[Answer] B。允许最小3岁开始配种，不能再小。

---

### 问题 2：牝马连续产驹间隔
现实中牝马一年最多产一匹（怀孕 11 个月），是否需要约束同一匹母马不能在连续两年都产驹？

**情景：** 母马 A 在 2024 年产了一匹，用户又想让她 2025 年再产一匹。

- A. 允许（架空设定自由度优先，不限制）
- B. 不允许同年产两匹，但允许连续年份（每年一匹）
- C. 现实约束：同一母马相邻后代至少间隔 1 年（即 2024 产一匹 → 最早 2025 再产）

[Answer] C

---

### 问题 3：种马死亡/引退后的处理
种马引退或死亡后（stud_year_end 有值），是否还能作为新架空马的父亲？

**情景：** Deep Impact 的 stud_year_end = 2019，用户想让一匹 2025 年出生的架空马的父亲是 Deep Impact。

- A. 禁止（严格按年份校验，2025 > 2019+1，不合法）
- B. 给出警告但允许（提示用户但不阻止）
- C. 允许（架空嘛，不做这个限制）

[Answer] A。在完全架空模式下，仅做弹窗提示，不禁止。

---

### 问题 4：后代出生年必须晚于父母
这个看起来显而易见，但需要确认：

- **后代出生年 > 父亲出生年**
- **后代出生年 > 母亲出生年**

是否还要更严格？比如后代出生年 ≥ 父亲出生年 + 3？

- A. 只要求晚于父母（> 父/母出生年）
- B. 要求合理间隔（≥ 父出生年 + 3，≥ 母出生年 + 4）
- C. 同 B，但如果父/母没有出生年则不校验

[Answer] B

---

### 问题 5：这些校验在什么时机触发？

- A. 创建/编辑马匹保存时校验，不合法则阻止保存
- B. 保存时校验但只是警告（黄色提示），不阻止
- C. 不在保存时校验，而是在血统表展示时标注"年份不合理"的提示
- D. B + C（保存时警告 + 展示时标注）

[Answer] D。我希望在出示血统表时注明是在什么模式下编辑的，这样能区分架空和严谨。

---

### 问题 6：未填写出生年的马如何处理？

**情景：** 用户创建架空马但没填出生年，系统能做年份校验吗？

- A. 出生年为空时跳过所有年份校验
- B. 强制要求填写出生年（否则不让保存）
- C. 出生年为空时跳过校验，但在血统表中标注"缺少出生年"

[Answer] A

---

## 总结

确认以上问题后，我将实现年份校验逻辑并集成到创建/编辑马匹的流程中。

---

## 补充问题（基于上轮回答）

### 问题 7：模式标记存储
你提到"血统表展示时注明是在什么模式下编辑的"。这意味着需要在每匹架空马的数据中记录它是在哪种模式下创建的。

- A. 在马匹数据中加一个 `created_mode` 字段（`"strict"` 或 `"free"`），创建时根据当前模式自动设置
- B. 不存字段，而是在展示时根据年份约束是否全部满足来动态判断（满足=strict风格，不满足=free风格）
- C. 同 A，但如果用户切换模式后编辑了该马，更新为最新的模式

[Answer] C

---

### 问题 8：严谨模式下，架空种马是否也需要设置 stud_year_start？
规则2要求种马有 `stud_year_start` 才能校验。但架空种马创建时，用户可能忘了填这个字段。

- A. 严谨模式下，角色为种牡马时强制填写 stud_year_start
- B. 不强制，但如果没填则该种马的后代不做配种年份校验（只做年龄校验）

[Answer] A
